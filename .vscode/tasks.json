{
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "gcc",
            "args": [
                "-g",
                "-o",
                "${workspaceFolder}/final/a.out",
                // "-Werror", // Optional
                // "-Wall", // Optional
                // "-Wextra", // Optional
                // "-ansi", // Optional
                // "-pedantic", // Optional
                "${file}",
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
    ],
    "version": "2.0.0"
}
